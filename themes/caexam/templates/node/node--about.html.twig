{#
/**
 * @file
 * Theme override to display a node.
 *
 * Available variables:
 * - node: The node entity with limited access to object properties and methods.
     Only "getter" methods (method names starting with "get", "has", or "is")
     and a few common methods such as "id" and "label" are available. Calling
     other methods (such as node.delete) will result in an exception.
 * - label: The title of the node.
 * - content: All node items. Use {{ content }} to print them all,
 *   or print a subset such as {{ content.field_example }}. Use
 *   {{ content|without('field_example') }} to temporarily suppress the printing
 *   of a given child element.
 * - author_picture: The node author user entity, rendered using the "compact"
 *   view mode.
 * - metadata: Metadata for this node.
 * - date: Themed creation date field.
 * - author_name: Themed author name field.
 * - url: Direct URL of the current node.
 * - display_submitted: Whether submission information should be displayed.
 * - attributes: HTML attributes for the containing element.
 *   The attributes.class element may contain one or more of the following
 *   classes:
 *   - node: The current template type (also known as a "theming hook").
 *   - node--type-[type]: The current node type. For example, if the node is an
 *     "Article" it would result in "node--type-article". Note that the machine
 *     name will often be in a short form of the human readable label.
 *   - node--view-mode-[view_mode]: The View Mode of the node; for example, a
 *     teaser would result in: "node--view-mode-teaser", and
 *     full: "node--view-mode-full".
 *   The following are controlled through the node publishing options.
 *   - node--promoted: Appears on nodes promoted to the front page.
 *   - node--sticky: Appears on nodes ordered above other non-sticky nodes in
 *     teaser listings.
 *   - node--unpublished: Appears on unpublished nodes visible only to site
 *     admins.
 * - title_attributes: Same as attributes, except applied to the main title
 *   tag that appears in the template.
 * - content_attributes: Same as attributes, except applied to the main
 *   content tag that appears in the template.
 * - author_attributes: Same as attributes, except applied to the author of
 *   the node tag that appears in the template.
 * - title_prefix: Additional output populated by modules, intended to be
 *   displayed in front of the main title tag that appears in the template.
 * - title_suffix: Additional output populated by modules, intended to be
 *   displayed after the main title tag that appears in the template.
 * - view_mode: View mode; for example, "teaser" or "full".
 * - teaser: Flag for the teaser state. Will be true if view_mode is 'teaser'.
 * - page: Flag for the full page state. Will be true if view_mode is 'full'.
 * - readmore: Flag for more state. Will be true if the teaser content of the
 *   node cannot hold the main body content.
 * - logged_in: Flag for authenticated user status. Will be true when the
 *   current user is a logged-in member.
 * - is_admin: Flag for admin user status. Will be true when the current user
 *   is an administrator.
 *
 * @ingroup templates
 *
 * @see template_preprocess_node()
 *
 * @todo Remove the id attribute (or make it a class), because if that gets
 *   rendered twice on a page this is invalid CSS for example: two lists
 *   in different view modes.
 */
#}
{% if not is_front %}
    <div class="container-fluid hero-area">
        <div class="row inner {{ row_class }}">
            <div class="col-lg-12 col-xs-12 col-md-4">
                <div class="form-outer">
                    <div class="quick-link">
                        <a href="/{{ content.field_link_1|field_value }}">
                            <img src="{{ content.field_link_image_1|field_value }}" alt="">
                            <span>{{ content.field_link_image_1|field_raw('title') }}</span>
                        </a>
                        <a href="/{{ content.field_link_2|field_value }}">
                            <img src="{{ content.field_link_image_2|field_value }}" alt="">
                            <span>{{ content.field_link_image_2|field_raw('title') }}</span>
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="container block-about">
        <div class="row">
            <div class="col-lg-4">
                <div class="wrapper">
                    <img src="{{ content.field_image|field_value }}" alt="">
                    {% set title = content.field_image|field_raw('title') %}
                    <h3>{{ title|raw }}</h3>
                    {{ content.body|field_value }}
                </div>
            </div>

            <div class="col-lg-4">
                <div class="wrapper">
                    <img src="{{ content.field_block_image_1|field_value }}" alt="">
                    {% set title = content.field_block_image_1|field_raw('title') %}
                    <h3>{{ title|raw }}</h3>
                    {{ content.field_block_body_1|field_value }}
                </div>
            </div>

            {% if content.field_translation_3rd_block.0 %}
                <div class="col-lg-4">
                    <div class="wrapper">
                        <div class="block-5">
                            {{ content.field_translation_3rd_block }}
                        </div>
                    </div>
                </div>
            {% else %}
                <div class="col-lg-4">
                    <div class="wrapper">
                        <img src="{{ content.field_block_image_2|field_value }}" alt="">
                        {% set title = content.field_block_image_2|field_raw('title') %}
                        <h3>{{ title|raw }}</h3>
                        {{ content.field_block_body_2|field_value }}
                    </div>
                </div>
            {% endif %}
        </div>

        <div class="row">
            <div class="col-lg-4">
                <div class="wrapper">
                    <img src="{{ content.field_block_image_3|field_value }}" alt="">
                    {% set title = content.field_block_image_3|field_raw('title') %}
                    <h3>{{ title|raw }}</h3>
                    {{ content.field_block_body_3|field_value }}
                </div>
            </div>

            <div class="col-lg-4">
                <div class="wrapper">
                    <img src="{{ content.field_block_image_4|field_value }}" alt="">
                    {% set title = content.field_block_image_4|field_raw('title') %}
                    <h3>{{ title|raw }}</h3>
                    {{ content.field_block_body_4|field_value }}
                </div>
            </div>

            <div class="col-lg-4">
                <div class="wrapper">
                    <img src="{{ content.field_block_image_5|field_value }}" alt="">
                    {% set title = content.field_block_image_5|field_raw('title') %}
                    <h3>{{ title|raw }}</h3>
                    {{ content.field_block_body_5|field_value }}
                </div>
            </div>
        </div>

        {% if content.field_table_1_title.0 %}
            <div class="row">
                <div class="col-lg-4">
                    <div class="block-2">
                        <h3>{{ content.field_table_1_title }}</h3>
                        <ul class="list-group">
                            <li class="list-group-item">{{ content.field_table_1_list.0 }}</li>
                            <li class="list-group-item">{{ content.field_table_1_list.1 }}</li>
                            <li class="list-group-item">{{ content.field_table_1_list.2 }}</li>
                            <li class="list-group-item">{{ content.field_table_1_list.3 }}</li>
                            <li class="list-group-item">{{ content.field_table_1_list.4 }}</li>
                        </ul>
                    </div>
                </div>

                <div class="col-lg-4">
                    <div class="block-2">
                        <h3>{{ content.field_table_2_title }}</h3>
                        <ul class="list-group">
                            <li class="list-group-item">{{ content.field_table_2_list.0 }}</li>
                            <li class="list-group-item">{{ content.field_table_2_list.1 }}</li>
                            <li class="list-group-item">{{ content.field_table_2_list.2 }}</li>
                            <li class="list-group-item">{{ content.field_table_2_list.3 }}</li>
                            <li class="list-group-item">{{ content.field_table_2_list.4 }}</li>
                        </ul>
                    </div>
                </div>

                <div class="col-lg-4">
                    <div class="block-2">
                        <h3>{{ content.field_table_3_title }}</h3>
                        <ul class="list-group">
                            <li class="list-group-item">{{ content.field_table_3_list.0 }}</li>
                            <li class="list-group-item">{{ content.field_table_3_list.1 }}</li>
                            <li class="list-group-item">{{ content.field_table_3_list.2 }}</li>
                            <li class="list-group-item">{{ content.field_table_3_list.3 }}</li>
                            <li class="list-group-item">{{ content.field_table_3_list.4 }}</li>
                        </ul>
                    </div>
                </div>
            </div>
        {% endif %}

        <div class="row">
            <div class="col-lg-4">
                <div class="wrapper last">
                    <img src="{{ content.field_block_image_6|field_value }}" alt="">
                    {% set title = content.field_block_image_6|field_raw('title') %}
                    <h3>{{ title|raw }}</h3>
                    {{ content.field_block_body_6|field_value }}
                </div>
            </div>

            <div class="col-lg-4">
                <div class="wrapper last">
                    <img src="{{ content.field_block_image_7|field_value }}" alt="">
                    {% set title = content.field_block_image_7|field_raw('title') %}
                    <h3>{{ title|raw }}</h3>
                    {{ content.field_block_body_7|field_value }}
                </div>
            </div>

            {% if content.field_translation_para_block.0 %}
                <div class="col-lg-4">
                    <div class="wrapper">
                        <div class="block-4">
                            {{ content.field_translation_para_block }}
                        </div>
                    </div>
                </div>
            {% else %}
                <div class="col-lg-4">
                    <div class="wrapper last">
                        <img src="{{ content.field_block_image_8|field_value }}" alt="">
                        {% set title = content.field_block_image_8|field_raw('title') %}
                        <h3>{{ title|raw }}</h3>
                        {{ content.field_block_body_8|field_value }}
                    </div>
                </div>
            {% endif %}
        </div>

        {% if content.field_translation_para_block.0 %}
            <div class="row">
                <div class="col-lg-4">
                    <div class="wrapper">
                        <img src="{{ content.field_block_image_8|field_value }}" alt="">
                        {% set title = content.field_block_image_8|field_raw('title') %}
                        <h3>{{ title|raw }}</h3>
                        {{ content.field_block_body_8|field_value }}
                    </div>
                </div>
                <div class="col-lg-4">
                    <div class="wrapper">
                        <img src="{{ content.field_block_image_extra|field_value }}" alt="">
                        {% set title = content.field_block_image_extra|field_raw('title') %}
                        <h3>{{ title|raw }}</h3>
                        {{ content.field_block_body_extra_1|field_value }}
                    </div>
                </div>
                <div class="col-lg-4">
                    <div class="wrapper">
                        <img src="{{ content.field_block_image_extra_2|field_value }}" alt="">
                        {% set title = content.field_block_image_extra_2|field_raw('title') %}
                        <h3>{{ title|raw }}</h3>
                        {{ content.field_block_body_extra_2|field_value }}
                    </div>
                </div>
            </div>
        {% endif %}

        {% if content.field_translation_para_block.0 %}
            <div class="row">
                <div class="col-lg-4">
                    <div class="wrapper last">
                        <img src="{{ content.field_block_image_extra_3|field_value }}" alt="">
                        {% set title = content.field_block_image_extra_3|field_raw('title') %}
                        <h3>{{ title|raw }}</h3>
                        {{ content.field_block_body_extra_3|field_value }}
                    </div>
                </div>
                <div class="col-lg-4">
                    <div class="wrapper last">
                        <img src="{{ content.field_block_image_extra_4|field_value }}" alt="">
                        {% set title = content.field_block_image_extra_4|field_raw('title') %}
                        <h3>{{ title|raw }}</h3>
                        {{ content.field_block_body_extra_4|field_value }}
                    </div>
                </div>
                <div class="col-lg-4">
                    <div class="wrapper last">
                        <div class="block-3">
                            {% set title = content.field_translation_table_title|field_value %}
                            <h3>{{ title|raw }}</h3>
                            <ul class="list-group">
                                {{ content.field_translation_table_list }}
                            </ul>

                            <ul class="list-group">
                                {{ content.field_translation_table_list_1 }}
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        {% endif %}

        {% if content.field_location_1_title.0 %}
            <div class="row">
                <div class="col-lg-6">
                    <div class="wrapper last">
                        <h3>{{ content.field_location_1_title|field_value|raw }}</h3>
                        <div class="map">
                            <iframe src="{{ content.field_location_1_url|field_value }}" frameborder="0" style="overflow:hidden;height:100%;width:100%" height="100%" width="100%" allowfullscreen></iframe>
                        </div>
                    </div>
                </div>

                <div class="col-lg-6">
                    <div class="wrapper last">
                        <h3>{{ content.field_location_2_title|field_value|raw }}</h3>
                        <div class="map">
                            <iframe src="{{ content.field_location_2_url|field_value }}" frameborder="0" style="overflow:hidden;height:100%;width:100%" height="100%" width="100%" allowfullscreen></iframe>
                        </div>
                    </div>
                </div>
            </div>
        {% endif %}
    </div>

    {% if content.field_partners.0 %}
    <div class="container-fluid white-area services">
        <div class="row inner">
            <div class="col-lg-4">
                {{ content.field_partners }}
            </div>
            <div class="col-lg-8">
                <div class="partners">
                    <div class="logo-slider">
                        {{ content.field_partners_images }}
                    </div>
                </div>
            </div>
        </div>
    </div>
    {% endif %}

    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-3 ic-1">
                {% set title = content.field_bottom_image_1|field_raw('title') %}
                <div><img src="{{ content.field_bottom_image_1|field_value }}" alt=""><span>{{ title|raw }}</span></div>
            </div>
            <div class="col-lg-3 ic-2">
                {% set title = content.field_bottom_image_2|field_raw('title') %}
                <div><img src="{{ content.field_bottom_image_2|field_value }}" alt=""><span>{{ title|raw }}</span></div>
            </div>
            <div class="col-lg-3 ic-1">
                {% set title = content.field_bottom_image_3|field_raw('title') %}
                <div><img src="{{ content.field_bottom_image_3|field_value }}" alt=""><span>{{ title|raw }}</span></div>
            </div>
            <div class="col-lg-3 ic-2">
                {% set title = content.field_bottom_image_4|field_raw('title') %}
                <div><img src="{{ content.field_bottom_image_4|field_value }}" alt=""><span>{{ title|raw }}</span></div>
            </div>
        </div>
    </div>
{% endif %}
